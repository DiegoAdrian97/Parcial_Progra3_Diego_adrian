<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <title>Detalle de Venta | Electronics Store</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&display=swap"
      rel="stylesheet"
    />
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: "Poppins", sans-serif;
      }

      body {
        background: linear-gradient(135deg, #0f0c29, #302b63, #24243e);
        color: #f1f1f1;
        min-height: 100vh;
        display: flex;
      }

      /* SIDEBAR */
      .sidebar {
        width: 260px;
        background: rgba(10, 10, 25, 0.98);
        border-right: 1px solid rgba(255, 255, 255, 0.08);
        box-shadow: 4px 0 20px rgba(0, 0, 0, 0.4);
        padding: 1.5rem 1rem;
        display: flex;
        flex-direction: column;
        gap: 1.5rem;
        position: sticky;
        top: 0;
        height: 100vh;
      }

      .sidebar-logo {
        font-size: 1.5rem;
        font-weight: 600;
        color: #b388ff;
        text-shadow: 0 0 15px #b388ff;
        margin-bottom: 0.5rem;
      }

      .sidebar-subtitle {
        font-size: 0.85rem;
        opacity: 0.75;
      }

      .sidebar-nav {
        display: flex;
        flex-direction: column;
        gap: 0.4rem;
        margin-top: 1rem;
      }

      .sidebar-link {
        display: block;
        padding: 0.7rem 0.9rem;
        border-radius: 10px;
        text-decoration: none;
        color: #e0dcff;
        font-size: 0.93rem;
        background: transparent;
        border: 1px solid transparent;
        transition: all 0.25s ease;
      }

      .sidebar-link:hover {
        background: rgba(179, 136, 255, 0.08);
        border-color: rgba(179, 136, 255, 0.5);
        box-shadow: 0 0 12px rgba(179, 136, 255, 0.4);
        transform: translateX(3px);
      }

      .sidebar-link.active {
        background: linear-gradient(135deg, #7b2ff7, #f107a3);
        box-shadow: 0 0 15px rgba(179, 136, 255, 0.7);
        color: #fff;
      }

      /* MAIN AREA */
      .main {
        flex: 1;
        padding: 2rem 3rem;
      }

      .main-header-title {
        font-size: 1.8rem;
        text-shadow: 0 0 12px #b388ff;
        margin-bottom: 0.5rem;
      }

      .main-header-subtitle {
        font-size: 0.95rem;
        opacity: 0.85;
        margin-bottom: 1.5rem;
      }

      .card {
        background: rgba(255, 255, 255, 0.05);
        border-radius: 15px;
        padding: 1.5rem;
        box-shadow: 0 0 18px rgba(0, 0, 0, 0.35);
        border: 1px solid rgba(255, 255, 255, 0.06);
        margin-bottom: 1.5rem;
      }

      .venta-info {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 1rem;
        margin-bottom: 1.5rem;
      }

      .info-item {
        padding: 1rem;
        background: rgba(0, 0, 0, 0.25);
        border-radius: 10px;
        border: 1px solid rgba(255, 255, 255, 0.08);
      }

      .info-label {
        font-size: 0.85rem;
        opacity: 0.75;
        text-transform: uppercase;
        letter-spacing: 0.05em;
      }

      .info-value {
        font-size: 1.3rem;
        font-weight: 600;
        color: #b388ff;
        margin-top: 0.4rem;
      }

      table {
        width: 100%;
        border-collapse: collapse;
        margin-top: 1rem;
      }

      thead {
        background: rgba(0, 0, 0, 0.4);
      }

      th,
      td {
        padding: 0.8rem 0.9rem;
        text-align: left;
        font-size: 0.9rem;
        border-bottom: 1px solid rgba(255, 255, 255, 0.06);
      }

      th {
        color: #e5e0ff;
        font-weight: 500;
      }

      tbody tr:hover {
        background: rgba(179, 136, 255, 0.08);
      }

      .btn-secondary {
        padding: 0.6rem 1.1rem;
        border-radius: 999px;
        border: 1px solid rgba(179, 136, 255, 0.6);
        background: transparent;
        color: #fff;
        text-decoration: none;
        display: inline-block;
        transition: all 0.25s ease;
      }

      .btn-secondary:hover {
        background: rgba(179, 136, 255, 0.2);
        transform: translateY(-1px);
      }

      @media (max-width: 900px) {
        body {
          flex-direction: column;
        }
        .sidebar {
          flex-direction: row;
          align-items: center;
          height: auto;
          width: 100%;
          padding: 1rem;
          position: static;
        }
        .sidebar-nav {
          flex-direction: row;
          flex-wrap: wrap;
        }
        .main {
          padding: 1.5rem 1.2rem;
        }
      }
    </style>
  </head>
  <body>
    <aside class="sidebar">
      <div>
        <div class="sidebar-logo">Electronics Store</div>
        <div class="sidebar-subtitle">Panel de administración</div>

        <nav class="sidebar-nav">
          <a href="/admin/dashboard" class="sidebar-link">📊 Dashboard</a>
          <a href="/admin/productos" class="sidebar-link">📦 Productos</a>
          <a href="/admin/ventas" class="sidebar-link active">🧾 Ventas</a>
        </nav>
      </div>
    </aside>

    <main class="main">
      <h1 class="main-header-title">Detalle de Venta #<%= venta.id %></h1>
      <p class="main-header-subtitle">
        Información del pedido y productos vendidos.
      </p>

      <section class="card">
        <div class="venta-info">
          <div class="info-item">
            <div class="info-label">Número de Orden</div>
            <div class="info-value">
              #<%= String(venta.id).padStart(4, "0") %>
            </div>
          </div>
          <div class="info-item">
            <div class="info-label">Cliente</div>
            <div class="info-value"><%= venta.cliente_nombre %></div>
          </div>
          <div class="info-item">
            <div class="info-label">Fecha</div>
            <div class="info-value"><%= venta.fechaFormateada %></div>
          </div>
          <div class="info-item">
            <div class="info-label">Total</div>
            <div class="info-value">$<%= venta.total %></div>
          </div>
        </div>

        <h2 style="margin-top: 2rem; margin-bottom: 1rem">
          Productos vendidos
        </h2>

        <table>
          <thead>
            <tr>
              <th>Producto</th>
              <th>Cantidad</th>
            </tr>
          </thead>
          <tbody>
            <% if (typeof productos !== 'undefined' && productos.length > 0) {
            %> <% productos.forEach(p => { %>
            <tr>
              <td><%= p.nombre %></td>
              <td><%= p.cantidad %></td>
            </tr>
            <% }) %> <% } else { %>
            <tr>
              <td colspan="2" style="text-align: center; opacity: 0.7">
                No hay productos en esta venta
              </td>
            </tr>
            <% } %>
          </tbody>
        </table>

        <div style="margin-top: 1.5rem">
          <a href="/admin/ventas" class="btn-secondary">Volver a ventas</a>
        </div>
      </section>
    </main>
  </body>
</html>
